# 2026-05-25

## code status → code sync

第一次 `code status` 显示 `docs/journal` 为 Clean，但远程实际有新 commit。可能 fetch 后 `remote_head` 没有正确参与比对。手动 `git merge --ff-only origin/main` 拉取。

再次 `code sync`，三段式输出：

```
docs/journal    ✓ push · sync · ✓ push-parent
```

最终 `code status` 确认 journal 变回 Clean，sync 正确。
